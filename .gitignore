# Secrets — never commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Node
node_modules/
dist/
.next/

# OS
Thumbs.db
.DS_Store

# Python
__pycache__/
*.py[cod]
.pytest_cache/

# dry-run previews are disposable, not decisions
data/dry-runs/

# simulation output - regenerable, not a record
sim-out/

# backtest output - regenerable, and the run log is a scratch artifact
data/backtests/

# vendored pytest installed by a sandboxed agent - never ours to ship
.test-deps/
